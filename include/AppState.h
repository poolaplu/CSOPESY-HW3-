#pragma once
#ifndef APP_STATE_H
#define APP_STATE_H

struct AppState {
    bool showTaskManager = false;
    bool showDesktop = true;
    // if ever may need pa iadd, lagay dito
};

#endif // APP_STATE_H